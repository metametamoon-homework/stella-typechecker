/*
TEST_BEGIN
{
  "type": "stop-on-first-error",
  "expected-error": {
    "code": "ERROR_UNEXPECTED_PATTERN_FOR_TYPE",
    "location": "17:7"
  }
}
TEST_END
*/

language core;

extend with #variants, #sum-types;

fn main(x : <| a : Nat, b : Bool |>) -> Nat {
  return match x {
      inl(n) => n
    | inr(b) => 0
  }
}