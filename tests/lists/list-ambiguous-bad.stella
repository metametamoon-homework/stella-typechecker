/*
TEST_BEGIN
{
  "type": "stop-on-first-error",
  "expected-error": {
    "code": "ERROR_AMBIGUOUS_LIST",
    "location": "16:23"
  }
}
TEST_END
*/

language core;

extend with #lists;

fn main(n : fn(Nat) -> Nat) -> Nat {
  return n(List::head([]))
}