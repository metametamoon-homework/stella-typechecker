/*
TEST_BEGIN
{
  "type": "stop-on-first-error",
  "expected-error": null
}
TEST_END
*/

language core;

fn wrap(x : Nat) -> (Nat + Bool) + Nat {
  return inl(inl(x))
}

fn main(n : Nat) -> (Nat + Bool) + Nat {
  return wrap(n)
}
