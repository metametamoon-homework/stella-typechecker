language core;

extend with #universal-types;

fn identity(x : Nat) -> forall X. fn(X) -> /*e=ERROR_UNDEFINED_TYPE_VARIABLE*/ Y {
  return generic [X] fn(x : X) {
    return x
  }
}

fn main(x : Nat) -> Nat {
  return identity(x)[Nat](x)
}
