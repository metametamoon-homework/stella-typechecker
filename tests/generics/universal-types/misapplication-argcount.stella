language core;

extend with #universal-types;

generic fn identity[T](x : T) -> T {
  return x
}

fn main(x : Nat) -> Nat {
  return /*e=ERROR_INCORRECT_NUMBER_OF_TYPE_ARGUMENTS*/identity  [Nat, Nat](x)
}