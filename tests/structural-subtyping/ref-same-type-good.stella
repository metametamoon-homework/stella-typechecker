/*
TEST_BEGIN
{
  "type": "stop-on-first-error",
  "expected-error": null
}
TEST_END
*/

language core;
extend with #references, #unit-type, #structural-subtyping;

fn use(r : &Nat) -> Unit {
    return unit
}

fn main(n : Nat) -> Unit {
    return let x = new(n) in use(x)
}
