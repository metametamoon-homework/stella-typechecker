/*
TEST_BEGIN
{
  "type": "stop-on-first-error",
  "expected-error": null
}
TEST_END
*/

language core;

extend with #records, #structural-subtyping;

fn main(f : fn({a : Nat}) -> {a : Nat, b : Nat}) -> fn({a : Nat, b : Nat}) -> {a : Nat} {
   return f
}
