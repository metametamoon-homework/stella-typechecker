/*
TEST_BEGIN
{
  "type": "stop-on-first-error",
  "expected-error": null
}
TEST_END
*/

language core;

extend with
  #top-type,
  #type-cast,
  #structural-subtyping;

fn inc(r : Top) -> Nat {
  return succ(r cast as Nat)
}

fn main(n : Nat) -> Nat {
  return inc(n);
}
