/*
TEST_BEGIN
{
  "type": "stop-on-first-error",
  "expected-error": null
}
TEST_END
*/

language core;

extend with
  #top-type,
  #type-cast;

fn applyToZero(x : Top) -> Nat {
  return (x cast as fn(Nat) -> Nat)(0)
}

fn main(n : Nat) -> Nat {
  return applyToZero(fn(m : Nat) { return m })
}
